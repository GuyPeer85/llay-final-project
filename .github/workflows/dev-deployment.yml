
name: dev

on:
  push:
    branches:
      - dev

jobs:
    call-workflow-deploy-dev:
      uses: GuyPeer85/testing-github-actions-llay/.github/workflows/CI-CD.yml@dev
      with:
        infa_env: dev
      secrets: inherit